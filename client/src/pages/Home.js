import React from "react";
// import { useQuery } from '@apollo/client';

// import {  } from '../utils/queries';

const Home = () => {
  return (
    <main>
      <h1>Welcome to the CRUD App with GraphQL and Redux</h1>
      <p>
        This is a simple MERN stack app that will be used to do CRUD methods on
        Users.
      </p>

      <p>
        I'm also showcasing conditional rendering based off of Authentication
        with JWT's.
      </p>
    </main>
  );
};

export default Home;
